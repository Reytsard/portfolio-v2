import React from "react";

function Profile() {
  return (
    <div className="h-full w-full text-white bg-gray-900">
      <div>
        picture,name,24|softdev,editprofile(might turn to download resume),
        followers,location,resume link, achievements
      </div>
      <div>
        pinned projects, projects, contributions title, contributions,
        contribution activity(school?,)
      </div>
    </div>
  );
}

export default Profile;
